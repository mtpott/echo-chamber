// profile will have user saved albums, recently commented albums, and friends list

import React from 'react';

function Profile() {
  return (
    <section>
      <h1 id="profile">Saved Favorites</h1>
    </section>
  );
}

export default Profile;